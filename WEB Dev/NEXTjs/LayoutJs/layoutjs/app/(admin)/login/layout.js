

export default function LoginLayout({ children }) {
    return (<div>
        <h1>Thus is Login button of admin page </h1>
        {children}
    </div>
    );
}


export const metadata = {        
  title: 'My LOGIN page',
  description: "bakwas page hai ye"
}